<%- title "Mulit-Meal" %>
<div class="container">
  <h1>New Meal page</h1>
  <div>
  <!-- Section of currently selction meal options-->
    <ul>
      <% @menu_ids.each do |menu_id| %>
      <li><%= Menu.find(menu_id).day_of_week %></li>
      <% end %>
    </ul>
    <ul>
      <% @user_ids.each do |user_id| %>
        <li><%= User.find(user_id).name_first %></li>
      <% end %>
    </ul>
    <ul>
      <li><%= @meal_type %></li>
    </ul>
  </div>
  <div>
  <%# Below is an issue. Not all meals have same course(s). Overwriting logic necessary %>
  <%# This would be the Meal partial for Course %>
    <ul>
      <%# <% @meals.each do |meal| %> %>
        <%# <li><b> <%= Meal.find(meal).id %> </b> %>
          <%# <% Meal.find(meal).courses.each do |course| %> %>
          <%# <li><%= course.course_type %></li> %>
          <%# <li><%= Course.find(course.id).erecipe_id %></li> %>
          <%# <% end %> %>
        <%# </li> %>
      <%# <% end %> %>
    </ul>
  </div>
<!-- Recipce search -->

  <%= render 'recipes/search_bar', path: new_meal_path %>
  <br>
  <%= render "recipes/recipes", recipes: @recipes, title: yield(:title) %>

</div>
