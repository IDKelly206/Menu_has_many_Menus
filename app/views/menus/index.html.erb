<h1>Menus#index</h1>
<p>Find me in app/views/menus/index.html.erb</p>

<div class="container">

<%= link_to "Create a new Menu", "#", class: "btn btn-ghost" %>

<% @menus.each do |menu| %>
    <div class="card-product">
      <div class="card-product-infos">
      <ul>
        <li><%= menu.date %></li>
      </ul>
      </div>
      <%= link_to "See Menu details", menu %>
    </div>
  <% end %>


</div>
