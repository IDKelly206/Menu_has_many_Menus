<%- title "Menu" %>

<div class="container">
  <div class="header">
    <%= link_to "Create Many Meals",
                new_planner_path,
                data: { turbo_frame: dom_id(PlannerForm.new) },
                class: "btn btn--secondary"  %>
  </div>
  <div class="section-menu">
    <%= turbo_frame_tag dom_id(PlannerForm.new) %>
    <%= render @menus %>
  </div>
</div>
