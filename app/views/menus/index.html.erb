<%- title "Menu" %>
<div class="body" id="<%= yield(:title) %>">
  <div>
    <%= link_to new_planner_path,
        data: { turbo_frame: dom_id(PlannerForm.new) },
        class: "new-meal"   do %>
        <i class="fa-solid fa-circle-plus btn btn--new_meal"> Plan a Meal</i>
    <% end %>
  </div>
  <div class="section-menu">

    <%= turbo_frame_tag dom_id(PlannerForm.new) %>
    <%= render @menus %>
  </div>
</div>
