<%- title "Menu" %>

<div class="container">
    <%# TurboFrameTag %>
  <div class="header">
    <%= link_to "Create Many Meals",
                meal_new_meals_path,
                data: { turbo_frame: dom_id(Meal.new) },
                class: "btn btn--secondary"  %>
  </div>
  <div class="section-menu">
    <%= turbo_frame_tag dom_id(Meal.new) %>
    <%= render @menus %>
  </div>
    <%# TurboFrameTag %>
</div>
