<%= render partial: "/recipes/load_more" %>
<%= turbo_frame_tag "recipes", target: "_top" do %>
  <div class="card-deck">
    <%= render recipes, locals: { title: yield(:title) } %>
    <div id="recipe_cards"
    data-target="course-type-radio-selector.assign"></div>
  </div>
<% end %>
