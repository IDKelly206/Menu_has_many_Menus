<% grocery_list.each do |item| %>
  <li>
    <%= form_with url: edit_multiple_groceries_path, method: 'patch' do |f| %>
      <%= f.label item[:n] %> |
      <%= f.label item[:q] %> |
      <%= f.label item[:m] %> |
      <%= f.label item[:cat] %>|
      <%= f.hidden_field :g_ids, name: "g_ids[]", multiple: true, value: item[:g_ids] %>
      <%= f.submit "Got it" %>
    <% end %>
  </li><hr>
<% end %>
