      <%= form_with model: [@menu, @meal, @course] do |f| %>
        <%= f.hidden_field  :erecipe_id, value: r.erecipe_id %>
        <%= f.hidden_field :course_type, value: params[:filters][:mealType] %>
        <%= f.submit "Add Course" %>
      <% end %>

<%= render "form", menu: @menu, meal: @meal, course: @course %>
