<%= form_with model: [@menu, @meal, @course] do |f| %>
  <%= f.hidden_field  :erecipe_id, value: erecipe_id %>
  <%= f.hidden_field :course_type, value: params[:filters][:dishType] %>
  <%= f.submit "Add Course" %>
<% end %>
